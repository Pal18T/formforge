'use client'

import React from 'react'
import { FormElementInstance } from './FormElements';

function FormSubmitComponent({formUrl, content}: {
    formUrl: string;
    content: FormElementInstance[];
}) {
  return (
    <div>FormSubmitComponent</div>
  )
}

export default FormSubmitComponent